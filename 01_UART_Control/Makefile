# 01_UART_Control - Makefile
#
# Builds the UART control phase against the shared Drivers and toolchain in ../.
# See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 01_UART_Control

include $(REPO_ROOT)/tools/build.mk
