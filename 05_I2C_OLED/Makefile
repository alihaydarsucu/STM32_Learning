# 05_I2C_OLED - Makefile
#
# Builds the I2C OLED phase against the shared Drivers and common boilerplate.
# See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 05_I2C_OLED

include $(REPO_ROOT)/tools/build.mk
