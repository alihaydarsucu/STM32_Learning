# 02_PWM_Output - Makefile
#
# Builds the PWM phase against the shared Drivers and toolchain in ../.
# See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 02_PWM_Output

include $(REPO_ROOT)/tools/build.mk
