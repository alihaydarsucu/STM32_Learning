# 00_LED_Blink - Makefile
#
# Builds the LED blink phase against the shared Drivers and toolchain in ../.
# See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 00_LED_Blink

include $(REPO_ROOT)/tools/build.mk
