# 03_ADC_Read - Makefile
#
# Builds the ADC read phase against the shared Drivers and toolchain in ../.
# See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 03_ADC_Read

include $(REPO_ROOT)/tools/build.mk
