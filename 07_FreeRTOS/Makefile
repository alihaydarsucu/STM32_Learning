# 07_FreeRTOS - Makefile
#
# Builds the FreeRTOS phase against the shared Drivers, common boilerplate
# and the FreeRTOS kernel checked into Middlewares/. See ../tools/build.mk
# for the shared build rules (it detects and compiles the FreeRTOS sources).

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 07_FreeRTOS

include $(REPO_ROOT)/tools/build.mk
