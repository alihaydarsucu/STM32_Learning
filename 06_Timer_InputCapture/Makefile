# 06_Timer_InputCapture - Makefile
#
# Builds the timer input-capture phase against the shared Drivers and common
# boilerplate. See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 06_Timer_InputCapture

include $(REPO_ROOT)/tools/build.mk
