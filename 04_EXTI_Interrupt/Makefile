# 04_EXTI_Interrupt - Makefile
#
# Builds the external-interrupt phase against the shared Drivers and common
# boilerplate. See ../tools/build.mk for the shared build rules.

PROJECT_DIR := $(abspath .)
REPO_ROOT   := $(abspath ..)
TARGET      := 04_EXTI_Interrupt

include $(REPO_ROOT)/tools/build.mk
